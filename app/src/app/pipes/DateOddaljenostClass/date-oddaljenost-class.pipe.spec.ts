import { DateOddaljenostClassPipe } from './date-oddaljenost-class.pipe';

describe('DateOddaljenostPipe', () => {
  it('create an instance', () => {
    const pipe = new DateOddaljenostClassPipe();
    expect(pipe).toBeTruthy();
  });
});
