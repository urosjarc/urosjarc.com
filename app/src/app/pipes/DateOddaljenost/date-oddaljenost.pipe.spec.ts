import { DateOddaljenostPipe } from './date-oddaljenost.pipe';

describe('DateOddaljenostPipe', () => {
  it('create an instance', () => {
    const pipe = new DateOddaljenostPipe();
    expect(pipe).toBeTruthy();
  });
});
