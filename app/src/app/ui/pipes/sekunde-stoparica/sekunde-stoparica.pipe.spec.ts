import { SekundeStoparicaPipe } from './sekunde-stoparica.pipe';

describe('StoparicaPipe', () => {
  it('create an instance', () => {
    const pipe = new SekundeStoparicaPipe();
    expect(pipe).toBeTruthy();
  });
});
