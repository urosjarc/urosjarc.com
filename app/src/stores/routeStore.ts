import {persisted} from "svelte-local-storage-store";
import {get} from "svelte/store";

export const route = {
  prijava: "/prijava",
  kontakt: "/kontakt",
  koledar: "/koledar",
  profil: "/profil"
}
